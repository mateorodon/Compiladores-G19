PROG_COMPLETO
begin
/#ESTE PROGRAMA RECONOCE LOS ERRORES PERO ALGUNOS SALEN REPETIDOS#/
    ulongint x,y;
    Single fun f3 (ulongint z)
    BEGIN
        outf(z);
    END;
    x := x + ;
    f3(x+y/);
    TYPEDEF TRIPLE SINGLE> t1;
    /#COMENTARIO DE PRUEBA#/
    FOR (i:=0; i < x UP 1);
end